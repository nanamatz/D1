# Play the Wor!d ??Project Instructions

Balatro-inspired word-building roguelite for the web. **The full design document is `docs/GDD.md` ??it is the single source of truth.** Every system referenced below cites its GDD section. Art direction is **pixel-art + CRT** (see `docs/UI_DESIGN.md`; the earlier ceramic/un-Balatro guardrail is retired).

## Architecture principles (non-negotiable)

1. **Headless engine.** All game logic lives in `src/engine/` and must never import DOM, React, or browser APIs. The engine must run in Node so `src/sim/` can autoplay thousands of runs for balance verification. UI (`src/ui/`) imports the engine, never the reverse.
2. **Jokers are data + hooks.** Every implemented joker (GDD 짠11) is a `JokerDef` object registering handlers on the event bus in `src/engine/events.ts`. Never hard-code a joker's effect inside pipeline code. If the pipeline lacks an event a joker needs, add the event. The public roster is Common 34 / Uncommon 57 / Rare 54 / Legendary 5 (150 total); the developer-only Primordial tile is registered separately and never enters production offers or Collection. Synesthete and the superseded Word-Hand-condition tiles are retired, and the separate 97-tile redesign remains postponed.
3. **No magic numbers.** Every tunable value lives in `src/engine/balance.ts`, keyed to GDD tables. New numbers go there first.
4. **Seeded RNG.** All randomness (bag shuffle, shop stock, packs, boss draw) flows through the implemented seeded RNG in `src/engine/rng.ts`. A run is fully reproducible from `RunState.seed`. Never call `Math.random()` in the engine.
5. **TypeScript strict mode.** The types in `src/engine/types.ts` encode GDD decisions (e.g. gibberish = `suit: null, posUsed: null`); keep them authoritative.
6. **Keep docs internally consistent.** When a patch changes a number or a mechanic, grep the docs for stale cross-references and update them in the same pass (e.g. changing the tile total means fixing every "N tiles" mention, not just 짠2.1; changing the settle/clear flow means updating the pipeline summary, not just the trigger section). Doc drift has caused real contradictions here more than once ??a value or rule must read the same everywhere it appears.

## Codex workflow

Codex works as a single agent for every task. **Do not spawn or use subagents, delegated role threads, parallel agents, nested `codex exec` processes, or background agent jobs.** The active agent owns investigation, planning, implementation, validation, review, documentation consistency, and the final report end to end.

## Key rules easy to get wrong (read before implementing)

- **Two-layer settlement (GDD 짠7.1):** individual word scores commit immediately; the sentence bonus is a *projection* overwritten every phase and only finalized at blind end. Never accumulate sentence bonuses per phase.
- **Owned Emoji Tile scoring order (changed 2026-09-03):** in the global owned-list `wordScoring` stage, each non-disabled physical owner resolves its Gray/Violet/Rainbow scoring edition before its own intrinsic effect and growth/decay beats, then advances left-to-right. Base/White add no scoring-edition beat. Tile-bound Emoji effects remain in the earlier played-tile phase, so Rare Earth and peers do not move; held-tile scoring remains after the complete owned list.
- **Conditional growth/decay presentation (changed 2026-09-04):** every conditional growth or decay trigger is presented immediately after its exact committed cause, before unrelated beats. For Glass, each actual destroyed material/result beat is followed by applicable growth beats in physical shelf order. Presentation never advances mechanics: Term Insurance and Type Foundry keep the pre-destruction value for the shattering word and apply growth from later words. Survival, Glass Insurance prevention, duplicate destruction attempts, and debuffs emit no destruction-growth beat. Preserve per-tile atomic order, instance/Echo attribution, event count, RNG, state, save shape, and `settleDurationMs()`; add no hook or timer merely to reposition an existing event.
- **Pattern matching (GDD 짠5.1):** whole-sequence match only; highest single pattern; articles/adjectives/adverbs are absorbed as modifiers (they don't break the skeleton, they add bonus). A gibberish hole voids all matches.
- **Run-scoped hidden sentence patterns (GDD 짠5.2, 2026-08-30; visual reference simplified 2026-09-04):** Run Info omits Object Complement, Ditransitive, and Complex entirely until the first successful actual submission whose existing post-boss `judgeSentence(sentenceSequenceForBlind(...))` result names that id as the highest pattern; Compound and the other eight remain public. Preview, failure, debuffed/removed words, and lower-ranked candidates never reveal. Preserve discoveries through the run/save/Continue, reset them on a new run, and never infer them from profile lifetime, Constellation levels, or finalized `patternPlayCounts`. Every visible pattern tooltip keeps its description and shows the 짠5.2 official example as uppercase word boxes only?봭o example heading, divider, role/POS labels, clause captions, arrows, or row breaks. Fill and border ordinary boxes with their selected POS tag colour; use a neutral marker tone for special grammar tokens such as ISNT rather than inventing a POS colour. List every example, including Compound and Complex, in one uninterrupted left-to-right row and horizontally scroll it only when the viewport is too narrow. Construct that UI-only content only after the discovery filter so hidden examples never enter the DOM or accessibility tree.
- **Run Info pattern-example spacing (changed 2026-09-04):** the one-row visual fills the tooltip width, centres the word-box group, and distributes boxes evenly; constrained viewports scroll horizontally without wrapping.
- **Rotating developer Laboratory (changed 2026-09-07):** the DEV-only Main Menu Laboratory is the single review surface for the latest completed player-facing implementation or change that cannot be inspected immediately in Collection or Run Info. This includes gameplay, audio, and animation work when those player-facing results meet that visibility criterion; internal-only refactors do not qualify. A new qualifying change replaces the prior contents; previews never accumulate, and automatic tests retain retired preview regressions. Reuse the real product component/data instead of copying it, and never add engine, RNG, run, discovery, profile, settings, or storage state for a preview. Preserve the shared 1440횞988 shell, Back/Escape, EN/KO, accessibility, responsive layout, and Forced Colors. The current surface reuses production `ScoreTypewriter` with deterministic UI-only Tier 1?? controls and Replay so the updated key counts, 10%-faster cadence, physical depression, LEDs, smoke, and flames can all be checked while resizing the viewport, without engine, RNG, run, discovery, profile, settings, or storage state. The former coffee-cup, money-ledger, money-total-motion, hidden-pattern, and word-score-transfer previews remain covered by automated regressions.
- **Gibberish (GDD 짠6.4):** always submittable; pays letter chips 횞 1.0; no suit multiplier; layer-1 joker hooks fire on it, layer-2/3 naturally don't (suit/POS are null).
- **No global minimum word length (GDD 짠6.5).** The old fixed floor stays retired. Stereotype Plate is the sole conditional boss exception: it blocks any hand shorter than the longest valid word already played this Chapter (GDD 짠8.3?벬?.4).
- **Bag never refills mid-blind (GDD 짠6.6)** ??this is the intended natural cap on any long-blind or future phase-extension build. Zombie is the sole explicit exception: after a play, it returns every played physical letter tile to the current blind's undrawn bag.
- **Discard budget is per blind, not per phase (GDD 짠6.3).** Discarded tiles *exit play for the rest of the blind* (Balatro-aligned) ??they do not return to the bag mid-blind; they come back only at blind end. Drawing replacements needs no RNG (they come from the remaining shuffled bag); the discard call still takes the seeded RNG for `discardGain` font-seal rolls (GDD 짠2.3).
- **Word Hands / ?⑥뼱 議깅낫 (GDD 짠5.5):** per-word structure bonuses fold into `WordScoringContext`: Chips add to the current word Chips and Mult multiplies the current word Mult; highest single hand only. The original six keep their rules; the knowledge tier adds Type Economy (8+ physical letters, all distinct), Vowelless (5+ physical letters, no canonical A/E/I/O/U), and Grand Palindrome (7+ physical letters). Y remains a consonant. The three new hands are valid-words-only and profile-secret until first completion. Emoji Tile text that says a hand is **contained** inspects raw structure independently of the highest-hand winner; Grand Palindrome therefore satisfies Mirror Image. Hand Scholar unlocks at 8 distinct hands in one run and its score factor is capped at 횞4. Engine ids remain `letterHands` / `LetterHandId`; discovery lives inside profile `wj.lifetime`, not a new save key.
- **Word length adds to Mult, it does not multiply (GDD 짠3.1, 2026-07-30):** `chips 횞 (suitMult + length 횞 BALANCE.wordLength.multPerLetter)`. **Valid words only** ??gibberish stays `chips 횞 1.0` (짠6.4), which also covers letterless Stone tiles (a word holding one always fails lookup). One helper, `wordLengthMult(letterCount, isGibberish)` in `src/engine/scoring.ts`, is the single source of truth: `scoreWord`, `loop.ts`'s `scoreSubmission`, and `engine/hint.ts` all call it. Forgetting `hint.ts` makes the Magnifier recommend the wrong word.
- **Auto-settle (GDD 짠7.2):** the blind auto-*detects* the clear (`pendingEnd` is set once projected ??target) and plays the full settle + sentence-finalize sequence ??the bonus must be seen pushing the score over. There is **no intermediate "Cleared! + Settle button" verdict screen** (removed, item 4): after the finalized score lands (Stage 1) the round number rolls up, and after a short verdict beat the blind auto-resolves to the **Fee Settlement** modal (its own Collect button confirms) or, on a loss, to Game Over ??`useGame`'s two-stage effect. The `earlyEndDisabled` flag still disables the early clear (settles only when phases run out) ??a dormant hook no boss in the current roster sets (GDD 짠8.3, 2026-07-21; the old "Perfectionist" is retired). Resolution is still gated on the settle-complete SIGNAL, never on the raw score (see the invariant below). The verdict beat is a fixed pace AFTER the signal, never a guess at settle length.
- **The clear UI is gated on the settle-complete SIGNAL, never on the score (playtest-05 A ??twice-recurring bug, treat as a first-class invariant):** the round-clear verdict *and* the Game Over UI may only appear after the settlement-sequence completion signal fires ??never off the final score value, which the engine knows instantly. "Settlement sequence complete" is the single event that gates them (this unifies 04 A-1 and A-2). Concretely: `SettleProvider` (`src/ui/settle.tsx`) fires `onComplete` when its timeline lands ??`useGame.markSettleComplete()` ??`state.settleComplete` gates both the verdict overlay and `finalize`, then a short beat. **Never re-introduce a fixed delay to "wait for the settle"** ??settle length is variable because its ordered score-event slots scale with beat count and game speed, while impact and final hold remain fixed; any guessed constant undershoots long words and fires the verdict early. That exact mistake caused both recurrences; `settleDurationMs()` is the one source of truth for the timing.
- **Voucher slot (GDD 짠9.2):** immune to reroll; ordinarily one purchase per chapter (`voucherLocked`), with Voucher Tag's surviving next-shop choice as the sole second-purchase exception; the offer is fixed per chapter (`run.voucherOffer`), restocked only when the Deadline/boss clears. Purchased vouchers never reappear; an unpurchased offer stays eligible but cannot repeat in the immediately following Chapter.
- **Blind skip + Editorial Perks (GDD 짠8.2, changed 2026-09-05):** Draft and Revision may be skipped; Deadline never may. Each Chapter's two seeded offers are fixed in `RunState.skipOffers` and fully disclosed on Blind Select; neither Tag id may repeat either id from the immediately preceding Chapter, so History Book/Old Book cannot restore the old pair when the same Chapter number returns. Skipping grants the current offer and advances directly with no scoring, Fee Settlement, clear/interest/remaining-resource payout, or Stationery Shop. The complete 30-entry pool and every value live in `src/engine/skipRewards.ts` + `BALANCE.skipRewards`; components never recreate effects. **Lead Story / ?쒖? 湲곗궗 is retired.** `isImmediateSkipReward` is the single immediate-timing classifier: an immediate Tag is acquired on Skip, plays its auto-activation/burst-away sequence, and only then runs the existing reward path; Supply Tag immediately creates up to two eligible Base Common Emoji Tiles with the seeded RNG and shared ownership/capacity gates; free-pack Tags open the normal `PackOpening` flow without a duplicate implementation. Delayed blind/shop/boss rewards persist until their named resolution. `isNextShopSkipReward` identifies shop-facing Tags: they stay visible through blind play and Fee Settlement, and disappear only when `applyPendingShopTags` reports them in `appliedTags` on actual Shop entry or a resolving reroll; a Tag that cannot resolve remains visible and pending. Reroll Tag resolves on shop entry and changes that visit's progression to $0, $1, $2??before ordinary discounts/floors. Voucher Tag adds a second next-shop choice and both may be purchased in that shop; the survivor is the sole exception to the one-purchase-per-Chapter lock. Next-blind bonuses stack across consecutive skips and clear only when Play is selected; Publicity persists until the next successful clear. A skipped guided Draft sets `showIntro=false` for that run so the YELLOW lock cannot attach to an unrigged hand. **Blind Select layout:** Select stays at the card top; the bottom reads `OR ??image tag + Skip`. All 30 effect-specific tag images live UI-only in `src/ui/assets/skip-rewards/`, are reused by Collection ??Tags, expose the perk through the shared tooltip, and use shared pointer tilt/lift/sheen; upcoming tags remain visible but inactive. A skipped card gets the strong striped/stamped treatment and its old Tag is non-interactive. Next-blind Tags in the uninterrupted chain use the maximum-two lower-right stack and burst on Play entry, except Scarlet Tag remains tooltipped and interactive throughout the played blind and disappears only when it exits to Fee Settlement or Game Over; next-shop Tags share that stack but burst on Shop resolution instead.
- **Sketch Book boss rerolls are Deadline-current only (feedback5, 2026-07-29):** Blind Select may preview the chapter boss from Draft onward, but the reroll control is rendered and accepted only when `run.blindIndex === 2` and that Deadline card is current. Keep the UI gate and `useGame.rerollBoss` guard aligned.
- **Boss roster (GDD 짠8.3?벬?.4, changed 2026-08-27) ??data + hooks, art in the UI:** ordinary Deadlines draw from 15 publishing-frame bosses (`wanted`, `letter`, `ancientPaper`, `forbiddenPaper`, `bond`, `historyBook`, `memoirs`, `budgetBook`, `contract`, `burntPaper`, `whitePaper`, `will`, `deadLetter`, `stereotypePlate`, `orphanLine`). Every Chapter divisible by 8 instead draws one of six finishers (`cleaningSign`, `medusa`, `nokdoScript`, `blueprint`, `vitalSign`, `ultrasound`), each paying $8. Core and finisher pools keep independent no-repeat cycles; previews and rerolls consume that cycle, and a boss may reappear only after its whole pool is exhausted. A scheduled finisher remains a finisher if History Book/Old Book lowers the Chapter; rerolls derive their pool from that boss id. Each is a `BossDef` with hooks (`handSizeDelta`/`targetMult`/`setup`/`enter`/`afterPlay`/`handChanged`/`sentenceSequence`/scoring/economy hooks); **never hard-code a boss effect in pipeline code** ??add a hook if the pipeline lacks one. Nokdo's forced tile may be transformed or destroyed by consumables and is then reconciled; Blueprint shuffles once, hides identities while effects remain active, and restores every face when the blind ends; Ultrasound's `bossDisabled` marker suppresses all hooks and editions until it rotates/clears. **"Debuff" short-circuits scoring to one 0-point settle before tile/material/font/edition, Word Hand, Emoji Tile, `wordChecked`, `tilesPlayed`, `wordScored`, Briefcase, score-side RNG, and score/progress ledgers.** Physical play/phase/draw and boss `afterPlay` still resolve; the valid word still counts for actual-play Collection/presentation unlocks and `wordsThisAnte`. Boss sentence transforms apply to raw history first?봑rphan Line therefore always removes the literal first submission?봳hen debuffed submissions are removed rather than turned into holes before Pattern/Unison/register-synergy judgment, so remaining eligible words join. Gibberish keeps its separate hole/layer-1 rules and voids all three judgments. The engine stays headless: **boss/blind emblem images live in `src/ui/bossArt.ts`**, never imported into the engine; `BossDef.emoji` is only a fallback.
- **Endless Mode (GDD 짠8.2, 2026-07-31):** Chapter 8 is still the win/unlock boundary. The Published screen may continue through its pending Fee Settlement into Chapter 9 with `victorySecured=true`; later losses do not erase that win. Chapters 9??8 use the `BALANCE.endless` double-exponential curve, Chapter 38 Deadline is the explicit endpoint, and Chapter 39 must never be constructed. No Infinity/NaN may enter state or persistence. Lifetime profile stats separately record highest Endless Chapter and best finalized Endless blind score.
- **Boss phase/discard presentation (changed 2026-07-29):** History Book reduces `phasesTotal` by 2 for that boss blind only (minimum 1); it does not mutate the run's phase baseline. Unopened Letter's seeded `discardOnPlay` result is returned by the headless submit result so the UI can animate the exact up-to-four removed tiles; the UI never re-rolls that choice.
- **Pouch remaining count (playtest-03 D-1; shop exception 2026-07-31):** during an active/prepared blind, "remaining" = the undrawn bag (`blind.bag`) ONLY ??tiles in hand, played, or discarded have left the pouch. The shop has no active blind and must show the complete permanent pouch (`run.bag`), never the completed blind's stale remainder.
- **Starting Pouches + cumulative Records (GDD 짠12, decided 2026-07-30):** the
  former starting-deck and stake/Ink-difficulty placeholders are retired. A run
  selects one of 14 Starting Pouches and one of 8 Records; Records apply every
  penalty from White LP through the selected level. Resource deltas add before
  floors (hand/phases min 1; discard/Emoji/consumable slots min 0). Purple
  Pouch pays $2 per remaining phase + $1 per remaining discard on a clear and
  **always has zero interest**. Green LP target growth is
  `횞1.15^(Chapter??)`; Briefcase balances each word and sentence bonus's final
  Chips/Mult axes independently after all hooks, using the unrounded arithmetic
  mean, and doubles targets. Coin Purse keeps 68 tiles and uniformly reassigns
  each tile A?밵 through the one seeded RNG (zero copies allowed). Custom-seeded
  wins earn no Pouch/Record unlocks. Starting vouchers are owned/applied from
  frame one, never reoffered, and do not consume the Chapter voucher purchase.
  Korean display is **二쇰㉧??*, never 蹂대뵲由? existing engine `bag` identifiers
  remain internal. Pouch/Record definitions, composition, unlock evaluation,
  and Lunch balancing stay headless/data-driven; components never recreate
  their rules. **New Run disclosure (changed 2026-07-31):** unlocked Pouches
  show no unlock copy; locked Pouches show `Locked / ?좉?` plus only their exact
  condition in the panel, while the tooltip retains actual name/effect. Record
  unlock conditions are never rendered in New Run or tooltips; Records have no
  Collection category.
  ordered dots and lock state communicate the ladder. Art stays UI-only and
  follows the shared pixel-object contracts in UI_DESIGN.
- **Challenges v1 are fixed headless Pouch + Record presets (GDD 짠12.1, 2026-08-22):**
  `src/engine/challenges.ts` owns exactly six ordered `ChallengeDef` rows:
  Red Pen=Yellow+Red LP, Rising Quota=Green+Green LP, Narrow Desk=Five-Color+
  Yellow LP, Three Passes=Leather+Clear LP, Balanced Burden=Briefcase+CD, and
  Random Final=Coin Purse+DVD. Reuse the existing Pouch-then-Record construction
  and `BALANCE`; never recreate effects in components or add Challenge-only
  balance, currency, rewards, or art. Challenge 1 starts unlocked, N completion
  unlocks N+1, completed entries stay replayable, and only the Chapter-8 Deadline
  win completes one. Challenge and custom seed are mutually exclusive; Start uses
  `randomSeed()` and replaces the normal single `wj.run`, whose optional known
  `challengeId` survives Continue. Unknown non-null saved ids are rejected.
  Challenge starts alone bypass ordinary Pouch/Record locks. Standard win rewards
  (Pouch unlock, per-Pouch Record ladder, Emoji Record sticker, balance telemetry)
  require `!customSeed && challengeId == null`; real-play Collection, presentation,
  Emoji/Voucher, lifetime, pattern, and owned-Emoji progress still count.
  Completion ids are known-only, deduplicated, and stored inside the active
  profile's existing `wj.lifetime`; add no save key and do not bump the run-save
  version for this optional field. Reveal All preserves genuine completions,
  fabricates none, disables every new Start, and lets an already-saved Challenge
  Continue without recording its eventual win.
- **New Run modal/tab contract (changed 2026-09-04):** one compact `720횞700px` border-box shared shell
  owns the tablist, internally scrolling content viewport, fixed `82px` action
  row with a maximum-`340px` primary button, reserved optional-note row, and one
  full-width Back. Production conditionally creates exactly New Run + Continue;
  DEV alone appends Challenge start from a DEV-gated dynamic import; its UI
  implementation chunk must not exist in a production build. Never CSS-hide a
  production Challenge tab.
  A known saved Challenge still displays its localized note and resumes unchanged
  in production. Continue reuses the real Pouch/Record renderers at their exact
  New Run sizes (`140횞140px` / `88횞88px` desktop and `92횞92px` / `68횞68px` at
  `??20px`) and reads only saved/run UI data: current Chapter/blind,
  committed score/target, Fee/?? best
  word, and seed?봭ever projected/final score. No engine/save/profile/RNG field is
  added for this presentation.
- **Terminology is display-strings only (GDD 짠1.2):** the publishing-frame glossary (Chapter/Draft/Revision/Deadline/Fee/Pouch?? lives in i18n; never rename engine identifiers (`blind`, `ante`, `BlindKind`) for it. Korean Chapter copy uses `N?? only when attached to a number (`1??, `8??); standalone uses `梨뺥꽣` (`?대쾲 梨뺥꽣`, `梨뺥꽣留덈떎`, `梨뺥꽣 ??`). Japanese uses concise game-UI copy and the fixed terms ?곥긿??/ ?띸럤 / ?뽧꺀?ㅳ꺍??/ 蘊뉔뇫 / ?앫꺖??/ ?с궠?쇈깋 / ?뉐엹 / ?с궦?밤궭??/ ?뉎궭?⒲깳. Simplified Chinese uses 嶺밭쟻 / ?띸럤 / ?꿩낏 / 葉욤뉩 / 耶쀨쥕 / ?긺뎴 / ?ε엹 / 瑥?윜 / 阿긺쟻. Traditional Chinese uses 映뚨♠ / ?띸럤 / ?꿩낏 / 葉욤꼇 / 耶쀨쥕 / ?긺뎴 / ?ε엹 / 沃욃윜 / 雅귞♠ and Taiwan UI terms. Spanish (Spain) uses Cap챠tulo / Borrador / Revisi처n / Fecha l챠mite / Honorarios / Bolsa / Disco / Ficha emoji / Galimat챠as / Mano de palabra / Papeler챠a. French uses Chapitre / Brouillon / R챕vision / Bouclage / Honoraires / Sacoche / Disque / Tuile emoji / Charabia / Main de mots / Papeterie. Russian uses Глава / Черновик / Правка / Дедлайн / Гонорар / Мешок / Пластинка / Эмодзи-плитка / Белиберда / Словесная комбинация / Канцелярская лавка. Polish uses Rozdział / Szkic / Korekta / Termin / Honorarium / Worek / Płyta / Płytka emoji / Bełkot / Układ słowny / Sklep papierniczy. Turkish uses Bölüm / Taslak / Revizyon / Son Teslim / Ücret / Kese / Plak / Emoji Taşı / Saçmalık / Kelime Eli / Kırtasiye. The display term for a joker is **Emoji Tile / ?대え吏 ???/ 永득뻼耶쀣궭?ㅳ꺂 / 烏ⓩ깄??/ Ficha emoji / Tuile emoji / Эмодзи-плитка / Płytka emoji / Emoji Taşı** (GDD 짠11, decided 2026-07-23); the engine identifier stays `joker` (`JokerDef`, `src/engine/jokers/`, `BALANCE.jokerSlots`, `RunState.jokers`), as do locale key *names* like `shop.yourJokers`.
- **Displayed round score = committed only (GDD 짠7.1, final-settlement axes clarified 2026-09-06):** the big round number is the committed score and NEVER decreases. It ALWAYS rolls up with one eased 700ms count-up (`useCountUp`, the same tween the sentence bonus uses) ??no per-beat stepping. While an ordinary word settles, the number holds at the pre-word committed (`committedBefore`) and the ScoreBox replays that word's Chips/Mult beats. During final sentence settlement the axes remain 0 횞 0, so the committed round score is never replayed as Chips or Mult. After the authoritative settle-complete signal, a positive finite final word score appears while Chips 횞 Mult reads 0 횞 0; both it and the round hold for 500ms at every game speed, then the word score counts down as the round eases to the new committed. Reduced Motion keeps the same static 500ms post-signal hold, then snaps. Invalid, zero, and negative gains add no hold. On a full-motion clear with a real sentence bonus, the word transfer runs alone from t500??200 while the ScoreBox remains 0 횞 0; BUILD then runs t1200??900, LAND runs t1900??600, and only then does the verdict beat begin. A positive no-bonus clear may publish `finalScore` at t500 because the existing resolution delay still covers the word transfer; zero or non-finite gain keeps immediate sentence processing. This UI-only timing is keyed by `settleId` and cancelled on mode change/unmount; it never changes `settleDurationMs()`, events, input, audio, RNG, engine, or save state. At blind end the round eases on to the finalized score. During play, show the current highest valid sentence-pattern name and live sentence-bonus score beside it as `pattern name : score`; never fold that projection into the round number. The engine still maintains projected score for auto-clear.
- **Reset ALL per-blind state before the next blind's first frame (playtest-04 B-1):** sequence, staged/discard selections, committed/projected, settle log (`lastEvents`/`settleId`), tray. A stale flash means the new blind rendered old state then reset late ??reset in the transition, not after.
- **Discards are limited by count-per-blind only (GDD 짠6.3, playtest-04 D-4):** there is NO per-use tile cap; one discard may dump any number of marked hand tiles.
- **Current feedback invariants (2026-09-02):** Korean display uses **??* and **???뺤궛**; English Fee and engine `gold` ids remain unchanged. Live Stereotype Plate surfaces reuse the exported headless current-minimum helper and show an exact number or no restriction at 0, while Collection remains generic. A blocked submission uses its own red/Mult warning class, distinct from ordinary warnings and preserved under Forced Colors.
- **Growing/dynamic effects (changed 2026-09-04):** Wood growth is applied exactly once by the headless submission path, and that one grown tile snapshot feeds the permanent pouch, active blind zones, sequence, submitted mini, and settle result; UI and simulations never recreate its rule. Concrete Wood tooltips show one combined current Chips total (letter Chips + live Wood Chips), while generic Wood references state growth only. The Scarlet Letter synchronizes to the discarded-A ledger and skips neutral 횞1 mutation/events. Scrap Dealer scoring and current additive-Mult tooltip reuse one headless permanent-pouch Brass helper, including a visible +0.
- **Presentation action/number rules (changed 2026-09-06):** `formatScore` owns main ScoreBox Chips/Mult and relevant money values/deltas: round once to integers; comma-format rounded absolute values below 10,000,000; from 10,000,000 use the largest `M/B/T/Qa/Qi` unit with at most one decimal truncated toward zero; retain compact scientific notation from `1e21`; and show an em dash for non-finite values. Semantic effect popups keep their own precision. ScoreBox keeps the authoritative Chips and Mult axes unchanged and sizes each already-formatted visible string independently: up to 7 characters 52px, 8?? characters 38px, and 10+ characters 30px; only the inner numeral clips, so score-pop VFX remain visible. Its display precedence is finalized sentence bonus fixed at 0 횞 0, active word settle, staged Word-Hand preview, then zero; a staged Hand exposes its exact Chips and Mult including 횞1 even when its secret name remains `???`, while debuffed/handless previews show zero. Every status variant stays vertically centred inside the fixed 52px slot; the enlarged Word-Hand label uses a safe line box and one-line ellipsis without changing the 188/211/234px score-panel or 484px allocation. Buy/Redeem are blue, Use/Use Now green, Sell red. Shop Buy/Redeem/Open retain the shared 44px action height and 82px minimum width regardless of the larger global blue-button rule. Toggle deselection is limited to reversible actionable surfaces; required single-choice settings stay non-nullable. The held-consumable card remains pointer-active while its action menu is open so a second click closes this reversible action menu. The global shared consumable vignette and Constellation level-up presentation are labelled body-portalled modal interactions: they inert `#root`, block background pointer/Escape/game shortcuts, trap focus while retaining their tooltip objects, and restore connected prior focus without changing their timers. An active Challenge appears in Run Info as a conditional full-width gold pixel banner between the header and tabs; normal runs reserve no space. ScoreBox/provenance spacing stays inside the fixed 188/211/234px and 484px contracts.
- **Money total motion (changed 2026-09-04):** each distinct displayed `MoneyValue` change restarts one transform-only motion on its nested total-text span: an increase hops upward for 480ms to `translateY(-4px)` with slight squash/stretch, while a decrease mirrors it downward to `translateY(4px)`. Initial mount, unchanged values, and both Reduced Motion paths do not move; the authoritative formatted total updates immediately with no layout shift. Existing signed pops remain. A Shop ledger-suppressed atomic transaction still moves the total once from its net delta while suppressing only the ordinary net popup and preserving the ordered ledger.
- **Shop Use Now money ledger (changed 2026-09-04):** only money-gaining Fables selected data-first by `FABLE_REGISTRY` effect kinds `doubleGold` and `jokerSellGold` replace the ordinary MoneyValue net popup. A successful atomic transaction presents the nonzero actual price as a signed spend beat first, then the nonzero full Fable payout as a signed gain beat; never net them. Goose derives payout from post-price gold and Heavenly Maiden keeps its existing cap. Keep one full-payout consumable vignette, queue rapid presentation events, and retain both ordered texts under Reduced Motion. The persistent Sidebar is the sole Shop balance and owns the single portalled ledger/live region only while `mode === 'shop'`; leaving Shop immediately hides and clears active or queued ledger presentation, while ordinary money pops continue outside Shop. Never coordinate ownership by mount order or module globals. Preserve final gold, validation, stock/consumption/counters, RNG/save state, and existing audio.
- **Will score cue (2026-09-02):** Will remains one final boss beat with Chips 횞0.5 and Mult 횞0.5. Only the target/clear Score Keyboard cue is gated by authoritative post-boss `projectedScore ??target`; ordinary typing and score-event order remain unchanged.
- **Multi-POS words (GDD 짠4.2):** a word like "taste" carries `pos: ['noun','verbTransitive',...]`; the sequence position it's played into resolves which single POS applies. Pattern matching should try all POS assignments and pick the best match (respecting the highest-pattern rule).
- **Modifier axes remain separate (GDD 짠2.2??.3, 짠11.8; changed 2026-08-05):** letter tiles carry `TileMaterial` (9, including the custom growing Wood material), `TileFont` (5), and `TileEdition` (base/gray/violet/rainbow). The persisted `TileFont` id `bold` displays as **Void / 蹂댁씠??*, using Jost 500 with CSS-expanded ink that closes its counters; never rename that engine id or old saves break. Emoji Tiles carry `JokerEdition` (base/gray/violet/rainbow/white) and never carry a letter material/font. Do not reuse one entity's union for the other. `RunState.jokerSlots` is base 5; Kung Fu Manual and White editions raise effective capacity. **Stacking & replacement (GDD 짠2.4):** the three letter-tile axes normally stack simultaneously; re-applying the SAME axis overwrites and discards the previous value (no refund, no stacking of two materials). Stone is the sole cross-axis exception: it hides and remembers the letter, resets the font to Medium, cannot receive a font enhancement, and restores only the letter when replaced by a non-Stone material.
- **Per-profile flags live together in localStorage, off the engine (feature-01/02; Palette Settings grant changed 2026-09-03):** tutorial seen-flags (`wj.tutorial`, `wj.tutorialIntro`), the word collection (`wj.collection`), and chromatic unlock ids (`wj.unlocks`) are all profile-scoped UI state ??never in `RunState`, never in the engine. **Chromatic unlocks are data-driven (GDD 짠13):** the word?뭙ffect registry is `src/ui/unlocks.ts`; presentation is applied by toggling `unlock-*` classes on `<html>`, notifying presentation-only subscribers, and gating the audio buses. **Never hard-code a word check in a component** ??add a registry row. Gibberish never unlocks (valid words only). Natural valid-word unlocks fire `unlockBus`/`ChromaticReveal` and enter the run-end recap. Settings ??Game's **Palette Convenience / ?붾젅???몄쓽??* action may instead grant only registry color/audio rows (currently RED/YELLOW/GREEN/BLUE/MUSIC/SOUND) to the current profile after an inline two-press irreversible warning; it preserves mascot/unknown ids and changes no other progress. After the profile write and active/saved-run recap baseline are durable, one atomic aggregate `unlockBus` event applies presentation and shows a single `ChromaticReveal`; it never emits six individual reveals and the synthetic ids never enter the run-end recap. Reveal All remains Profile-only for the broader all-progress escape hatch and marks Challenges disabled. There is no device-wide override. **"Truly monochrome" model:** the WHOLE palette in `tokens.css` defaults to neutral greys (not just accent tokens) and each token is mapped to one of the 4 color groups (RED/YELLOW/GREEN/BLUE) that restore it; a `world-mono` class on `<html>` (toggled by `applyPresentation` while no color is unlocked) also `filter: grayscale(1)`s `.frame`/`.screen` to catch hard-coded fills. The fixed `<CrtOverlay/>` sits outside those containers so it's never greyscaled.
- **Machine-local speed/audio settings (ident input clarified 2026-08-31):** public game speed is exactly `0.5횞 | 1횞 | 2횞 | 4횞` in that order, with `1횞` as the default. Exact supported numeric stored values are preserved; strings, non-finite values, and unsupported numbers normalize to `1횞`, while an already-persisted `2횞` remains `2횞`. The Audio tab note is exactly `Audio / ?ㅻ뵒??. Music/SFX share a responsive four-column `label | slider | value | mute` grid; both native checkboxes visibly read `Mute / ?뚯냼嫄? but have bus-qualified accessible names. New-format `musicMuted` + `sfxMuted` ignore stale `master`; otherwise one legacy migration folds a valid positive master into each group, while master `0` preserves both group sliders and mutes both. The next normalized write omits `master`. Effective gameplay audio is Palette bus gate AND not-muted, then group volume and recipe gain. The Sweet Turtles developer ident remains a separate fixed-output audio context and always renders its original colours with `filter: none`, independent of profile unlocks and gameplay audio settings. Its black preparing frame consumes but ignores input; once the ready logo or fallback text is visible, the whole ident is one localized accessible button surface that accepts ordinary keyboard input, primary pointer/touch/pen, or an accessibility click. That gesture must never reach the delegated gameplay `buttonPress`/`audio.unlock` path, and finishing synchronously disposes every ident timer and audio reservation/context.
- **Tutorial encounters are WooDak spotlight coach-marks (feature-02 override; live tracking corrected 2026-09-03):** every encounter in `ENCOUNTERS` (`src/ui/tutorial.ts`) carries `mascot: 'woodak'` + a `target` selector and renders via `SpotlightBubble` ??EXCEPT `shopFirstVisit`, which keeps `mascot: 'piyak'` (her shop-owner greeting, GDD A-2). `SpotlightBubble` falls back to a centered bubble if the target isn't in the DOM. Intro-step selectors must point at a normal-sized element, never the whole `.frame` (the spotlight would cover everything and push the bubble off-screen). **The overlay is `createPortal`-ed to `document.body`, and must stay that way:** during the monochrome start `.frame` carries `filter: grayscale(1)` (world-mono) and `#root` carries `zoom` ??EITHER makes an ancestor a containing block for a `position: fixed` child, so an overlay rendered inside the board is positioned relative to `.frame` (offset ~80px), not the viewport (playtest bug: spotlight pushed off the target). Portalling to body escapes both, so `getBoundingClientRect()` maps 1:1 to the fixed overlay. The coach-mark tracks its target every animation frame (re-rendering only on change) and applies the existing 8px padding, with no `top`/`left`/`width`/`height` transition; the box follows the live rect on the next paint instead of chasing it through a positional tween. Because the portal escapes the world-mono filter, the coach-mark mascot is re-greyscaled explicitly via `:root.world-mono .intro-mascot .mascot-cat` (tokens.css) to stay B&W during the colourless start.
- **The guided first-run intro is a hard-locked YELLOW lesson (dynamic action cue changed 2026-08-30; tracking corrected 2026-09-03), not a passive tour:** `INTRO_STEPS` (tutorial.ts) is 4 steps ??frame ??discard ??build ??submit ??with `advance: 'next'|'discarded'|'staged'|'played'`. All three gated steps reuse `.tutorial-action-target`; `StagePanel` marks exactly one current target with its left/right click kind: the unmarked spare uses right-click, then enabled Discard uses left-click, each next physical YELLOW tile uses left-click, and enabled Play uses left-click. `SpotlightBubble` retains its body portal and per-RAF target lookup/rect tracking without a positional tween, so each new action target is followed on the next paint without hover; it shows a neutral 40횞40 CSS mouse cue only for fine pointers, outside but within 48px of that rect, and the cue is static under Reduced Motion. The opening hand is **rigged** for the tutorial run only (`!hasSeenIntro() && readTips()`): `bootstrap` passes the generic `openingLetters: 'YELLOW'.split('')`. The first six physical tile IDs are protected; the next opening-order tile is the sole discard target. Only a successful ordinary `discardTiles` action advances, spends one real discard, and refills from the seeded bag. During that step all other staging, marking, sort, drag, and Play actions stay locked; build then enables only the exact protected Y?묮?묹?묹?뭀?뭌 IDs and relocks discard. A completed prior discard, missing spare, or exhausted budget resumes/falls through to build without a second charge or softlock; Skip clears the UI-only mark and releases the lock. The target remains 300, so YELLOW scores 252 (36 chips 횞 (1.0 + 6 length), 짠3.1), ends the lesson, and does not clear the blind. `TutorialHost` suppresses encounter popups until `hasSeenIntro()`. Keep tutorial policy in the UI: no engine tutorial branch, event, save key, or version change. `tests/yellow-lesson.test.ts` guards the physical-ID, real-discard, determinism, and target invariants.
- **PaletteGuide is separate from tutorials (changed 2026-09-03):** after the guided YELLOW intro is complete, Tips-on play may show one non-blocking required-Palette reminder after the first normal settle of each Chapter. It targets only the still-locked color/audio registry rows, cycles them in registry order without repetition until the locked set is exhausted, and uses session memory only?봭ever engine RNG, `RunState`, or `rngCounter`. A required unlock on that submission or a blind-ending settle skips the Chapter. Pause, Run Info, Boss Intro, entry effects, coach-marks, chromatic reveal, and every non-playing phase gate it. The `document.body` portal is a physical-viewport top-right button/status surface; click/Enter/Space dismisses it, hover/focus pauses its 6-second hold, full motion uses 240ms in/180ms out, and either Reduced Motion path removes travel/fade. Unlock, Tips-off, screen exit, and unmount cancel it immediately.
- **Mascot art resolves through `mascotSrc(role)` ??never import `woodak.png` at a call site (changed 2026-08-20):** every full-body WooDak render site (`WooDakMascot`, `SpotlightBubble`, `TutorialPopup`) gets its image from `mascotSrc('woodak' | 'piyak')` in `src/ui/mascots.ts`. The mascot hand cursor is the one exception to that full-body resolver: its normal/hover/active monochrome+colour URLs belong to the same `WOODAK_SKINS` row and resolve through the same selected+unlocked+art-backed fallback, never through a component skin switch. Piyak is fixed and never supplies a cursor; WooDak applies the player's chosen **skin** ??the chromatic-unlock mascot variants (GDD 짠13: ALIEN/GHOST/DOG plus TURTLE) are WooDak *skins*, selectable in **Collection ??Mascots** once unlocked **and** art exists; Settings no longer owns the picker. **Adding a skin = fill in its registry art and cursor fields, never a hard-coded word check.** All current variants (ALIEN/GHOST/DOG/TURTLE) have art. **ALIEN/GHOST/TURTLE must each read immediately as an independent character of that exact theme, using project-created local pixel art. Preserve the shared palette and line weight only; never import, trace, or closely imitate a recognizable third-party mascot or arcade sprite.** (CAT was retired from the roster 2026-07-22.) The selection remains `settings.mascot` internally (persisted in `wj.settings`); `mascotSrc` reads it live from storage (like `readTips()`) so long-lived tutorial hosts aren't stale. Profile Reveal All writes the real mascot unlock ids into that slot, so the same `activeUnlocks` path makes those skins selectable and refreshes the cursor resolver.
  **Mascot dialogue resolves through `voicedKeys(line, role)` (same file) ??never write a
  `voice.*` locale key at a call site.** It returns an ordered key chain that `t()` (which
  now accepts `string | string[]`) resolves, with `voice.woodak.<line>` always tailing the
  chain so a skin that hasn't written a line degrades to WooDak's copy instead of a raw key.
  Adding a mascot voice = adding `voice.<skin>.*` rows, never a component change. Piyak is a
  fixed role (`voice.piyak.*`). Encounter **titles** stay `tutorial.<id>.title` ??they are
  terms, not dialogue ??and `intro.step.*` is always WooDak (the guided intro only runs on a
  profile with no unlocks, so no skin can ever speak it).
- **Sentence-pattern scoring applies Chips then Mult (GDD 짠5.2, changed 2026-08-27):** every pattern owns `{ baseChips, baseMult, levelChips, levelMult }` in `BALANCE.patterns`. At settlement, pattern/modifier/Unison Chips add to committed, an active mixed-register synergy multiplies that combined Chips axis and materializes its gain into sentence Chips, then ordinary hooks and pattern/Unison/effect Mult resolve: `final = (committed + sentenceChips) 횞 sentenceMult + scoreBonus`. Constellation cards raise both pattern axes uniformly; Standard Unison adds Chips and register Unisons multiply Mult. Unison has priority over mixed-register synergy; Tower of Babel is therefore Unison-only.
- **Sentence bonus presentation preserves provenance (feedback5; score-panel layout changed 2026-09-04):** when BUILD publishes an authoritative finalized pattern after settle completion, show one localized 1.7-second central `Lv.N 쨌 [pattern icon] pattern name` notice using the Not Allowed title-animation family; live projection, ordinary settles, and Unison-only finals never show it. The sidebar's already-reserved live-pattern line becomes the finalized headline: pattern-only `Lv.N 쨌 [icon] Pattern`, Unison-only `[suit] Unison +Chips|횞Mult`, Mixed-only `[synergy] 횞factor Chips`, or pattern plus the winning style after ` 쨌`. Unison wins defensively if both style fields exist. Style colour applies to the complete headline (Unison suit token, Mixed Chips token); pattern-only stays gold, while visible names/axes/values and one complete accessible label keep colour non-authoritative. Do not render a duplicate score-box stamp. The score panel always reserves its 52px status/transfer slot, including an empty slot during BUILD, so the ScoreBox never moves. The headless `SentenceBonusBreakdown` supplies only three possible supplemental full-width rows: Modifier, one grouped Effects row preserving every non-neutral Chips/Mult/flat-score axis, then Pouch. Zero or one row keeps score/controls at 188/296px; two use 211/273px; three use 234/250px, always totaling 484px. Keep the finalized snapshot through BUILD, LAND, verdict, and live Fee Settlement; Collect clears it and any still-running notice in the existing same-transition Shop reset. Do not persist it in the run save.

- **Every rendered object is hoverable and tooltipped ??no exceptions (screens-spec 짠0):** any surface that draws an Emoji Tile, consumable, letter tile, voucher, pack, Starting Pouch, or Record ??including items revealed inside an opened pack ??wires the shared tooltip and cursor-reaction. Record tooltips describe the selected level's added penalty and mark the ladder cumulative. Shipping a new surface without them is a bug. Reusable Emoji Tile, Pouch, Record, and Voucher renderers own `TiltCard` motion by default; pass `motion={false}` only beneath an existing composite motion owner, and never nest motion owners. Static preload/decorative/VFX art remains raw. **Tooltip text never inherits its subject's styling** (a Light Italic tile gets a normal-weight, full-contrast tooltip); the tooltip describes the object, it does not imitate it. **Tooltips are portalled to `document.body` at the highest UI layer and must never be clipped or covered by a panel. Object tooltip panels are pointer-transparent: only the anchor owns hover, so moving off the object closes its tooltip even when the pointer crosses the panel. Run Info pattern examples are the sole pointer-interactive tooltip exception because their constrained example row must scroll.**
- **Supplemental tooltip packing (feedback3, containment changed 2026-09-06):** one additional definition prefers the left. With two or three, fold exactly one into the main tooltip and keep the rest together; fold priority is **material ??font ??edition**. Exception: when one description references exactly three Emoji Tile editions, all three edition definitions stay together (for example The Cowherd and the Weaver Girl). If another definition also exists, only the highest-priority non-edition definition may fold. Automatically referenced font/Gibberish definitions participate in the same final count. The stack moves right when only that side fits and folds inside the main card when neither side fits; translated copy uses last-resort wrapping and viewport containment, never overflow.
- **Emoji Tile art matches the live card footprint (GDD 짠11, UI_DESIGN 짠3):** the current shared runtime frame is **124횞165px** (near-3:4), used by shop offers, owned Emoji Tiles, held consumables, and Collection Emoji Tiles. Existing 84횞112 pixel masters scale with `object-fit: contain` + `image-rendering: pixelated`; new art should target the runtime frame and must never crop or distort. Never substitute the 5:7 consumable-card canvas or the `244횞400` Pack canvas.
- **Emoji Tile rendering is image-only and Pac-Man-style throughout (changed 2026-07-30):** the existing 30 masters in `src/ui/assets/jokers/` are the canonical visual references for every new entry. Match their deep navy playfield, white/red/yellow/cyan-led 3??-color palette, large blocky silhouette, hard un-antialiased pixel edges, and shared sprite scale/line weight. No painterly object icons, gradients, shadows, scenery, texture, lighting, or tiny decoration. ?쏱ac-Man-style??means this common early maze-arcade sprite grammar, never copies of Pac-Man characters, ghosts, maze layouts, or other original assets. The image fills the 124횞165 runtime card: no wrapper, persistent label, edition badge, or rarity-colored border. Apply shared idle motion and cursor tilt/sheen directly to the image; name/effect/rarity stay in the tooltip.
- **Score-event presentation order is fixed (feedback2; Score Keyboard feedback changed 2026-09-07):** whole-word stamps ??played tiles first-to-last (base tile, its enhancements, then per-tile Emoji triggers) ??owned Emoji Tiles first-to-last ??held tiles in the visible order frozen when Play is pressed ??future consumable hooks ??global/boss beats ??**Briefcase's final Pouch-axis balance beat, when active**. Held Brass therefore multiplies the current total Mult after every owned Emoji Tile effect. The current letter tile lifts and bounces for every tile-specific contribution. Its own base/material/font/edition value prints above it; an Emoji-authored contribution uses the letter tile id only for that motion and prints its value once below the firing Emoji Tile. Score-panel-only feedback is still a bug. The Score Keyboard resets to Tier 0 per `settleId`; only a positive increase in the current submission's local `max(0, Chips 횞 Mult + flat score)` can activate a beat. Its tier numerator adds the settle-id-snapshotted visible sentence assist `max(0, projectedScore ??committedScore)` (zero under `previewHidden`) and remains Idle below 1횞, using Tier 1?? boundaries `[1, 1.2, 1.3, 1.4, 1.5]`; committed/earlier scores and later BUILD/LAND never enter directly. Decreases, zero/gold/stat beats, invalid values, and sentence assist alone cannot demote or promote it. Normal settles idle on completion. Tiers use 5/10/12/16/20 visible caps and 3/4/5/6/7 audible presses, with a clearly visible 4px/.86 physical depression. Score Keyboard audible slots resolve the actual key from the visual sequence and use the 32 locally bundled WAVs: registry index i maps to sample (i mod 32) + 1, unknown ids use main Enter, samples load only when played, and failures fall back to the matching existing synthesis; ambient DeskObjects keep their synthesized `deskKeycap`. An actual clear with a non-zero peak repeats the existing full keyboard beat after settle completion through BUILD/LAND/count-up/verdict and the live Fee Settlement row/total animation: immediate cycle 0, then 10%-faster fixed `418ms 횞 [2,1.5,1.25,1,1][tier]`; main Enter's mapped WAV at the `deskEnter` accent gain is first, and each deterministic fill key's mapped WAV at the ordinary `deskKeycap` gain completes the tier count, Tier 1?? shakes locally, Tier 4 smokes, and Tiers 4?? add flame/POP. Collect's transition to Shop, terminal, next blind, unmount, or Reduced Motion ON cancels current/future repeat work synchronously. Temporary settle/tile/Emoji VFX still end on their ordinary timelines; loss, Tier 0, a true local-zero sentence-only clear, and nonclear never loop. Target crossing remains one logical once-per-blind event even though celebration cycles intentionally reuse Enter's mapped WAV at the `deskEnter` accent gain; Reduced Motion retains only its existing target-cross audio exception and a static peak/Enter accent. Clear repeats never trigger global shake or extend settle timing. Game speed scales only ordered score-event slots: ordinary slots are `600ms / speed`, enhanced owned-Emoji-edition slots are `1000ms / speed`, with the 600ms Lead Plate chance floor and 480ms creation-slot floor retained. The submitted-row impact/contact stays fixed at 650ms with contact at 40%, the final tally hold stays fixed at 650ms when score beats exist, and reduced motion stays fixed at 700ms. Score Keyboard Tier 1?? key/repeat timing and fixed target timing, tile/source VFX, audio, shake, transfer, BUILD/LAND, notices, verdicts, modals, and transitions remain at their 1횞 timing and intensity; each feedback effect still begins on its source score event. `settleDurationMs()` remains fixed impact + scaled event slots + fixed final hold, and the submission snapshots speed only for event cadence. Reduced Motion ON may collapse the active submission to its fixed branch; turning it OFF applies from the next submission.
- **An open Fable or Ink Pack exposes its pouch candidates to held Fables and Gamblers (changed 2026-08-20):** candidates are selectable immediately, before choosing a dealt card. A compatible tile-targeting Fable or field-dependent Gambler already in the persistent shelf may consume against that active field; direct tile-axis changes play the same preview-before-commit sequence, while field-wide/random effects use the shared result vignette. Field-independent Gamblers ignore candidate selection and keep their ordinary engine preconditions. A held use consumes only that held card, revalidates against the live candidate row at commit, and never spends a pack pick or adds created/copied tiles to the fixed candidate row. Preview and delayed commit must replay the same captured seeded action key; the pending transaction blocks conflicting shelf mutations, advances `rngCounter` exactly once only on success, and cancels without consuming RNG or leaving a lock. A pack close request establishes its gate immediately, cancels any pending held use, and must still commit the queued pack transition after the close animation. Outside this pack-only exception, held targeted Fables and field-dependent Gamblers use staged blind tiles.
- **Entrance animations chain off transition completion (screens-spec build notes):** a screen transition owns the screen until it finishes; the incoming screen's own entrance (hand draw, shelf fill, boss stamp) starts only after it. Never run them concurrently ??same discipline as gating the clear verdict on the settle-complete signal.
- **Loading preloads only the first interaction (screens-spec 짠2.0):** fonts, the active WooDak sprite, its selected skin's six tiny cursor states, default Yellow Pouch, White LP, and first Draft emblem may block the loading screen. Other cursor skins keep the current hand visible until their six files decode; other Pouch/Record art, Collection galleries, and pack/card registries load on demand. Never eager-glob every art asset, because deployment cold starts otherwise download and decode tens of MB before play.
- **Emoji Tile ownership is unique by default (GDD 짠9.2, feedback4; Copy Editor changed 2026-08-26):** every offer and every final acquisition path?봲hop, Charm Pack, Fable creation, or random/direct grant?봫ust pass the shared `canOwnJoker` gate. Stale offers must be rejected again at purchase/pick time. **Copy Editor** is the sole persistent capability that permits duplicate offers/acquisitions while owned; **Boar** is the sole one-shot exception and directly keeps one owned Emoji Tile plus one complete copy while destroying the rest (짠10.3). Echo Chamber copies Copy Editor's permission only while it resolves Copy Editor immediately to its right. Only effect text that explicitly permits copying may add another exception; never bypass the shared gate ad hoc.
- **The desktop build has no network and loads over `file://` (desktop packaging, 2026-07-29; macOS release withdrawn 2026-08-24):** the game ships as a Windows x64 Electron app (`desktop/`); macOS, Linux, and SteamOS are unsupported. It loads `dist/index.html` directly. Three kinds of change work perfectly in a browser and break **only** on desktop, so they are invisible during development: (a) a new CDN dependency ??a font, script, or remote image; (b) loading data with a runtime `fetch()` ??the dictionary and lexicon are bundled at build time via `?raw`/JSON imports in `src/ui/lexicon.browser.ts` and must stay that way; (c) making Vite's `base` absolute again ??it is `'./'` and must stay relative so the same build runs from `file://`, the gh-pages `/D1/` subpath, and itch.io. `scripts/check-offline.mjs` (run by every `build:desktop*` script) catches (a) and (c) automatically; (b) is on you. **The Electron app name `Play the World` (`app.setName` in `desktop/main.js`) must never change** ??Windows `userData` is keyed by that name, so renaming it orphans saves and localStorage. `desktop/` imports no game code; the dependency direction is one-way (`desktop/ ??dist/`). The explicit x64 package output stays outside the repo at `../D1-release/win-unpacked/Play the World.exe` (see `docs/superpowers/specs/2026-07-29-desktop-packaging-design.md`).
- **Desktop Video resolution (changed 2026-09-03):** Settings has exactly Game / Video / Audio tabs. Video orders Resolution before Fullscreen, UI scale, and the former Graphics CRT controls. The Electron main process alone validates and applies the four windowed content-size presets (960횞600, 1280횞800, 1600횞900, 1920횞1080) through a main-frame-only context-isolated bridge; all choices are visually equal. Include the native frame when disabling presets that exceed the active work area, restore from maximized before sizing/centring, disable Resolution in native fullscreen, and report unmatched manual sizes as Custom. Browsers show disabled browser-managed Automatic and never call `resizeTo`. `window-state.json` remains authoritative; add no resolution field to `wj.settings`, engine state, profile progress, or save keys. The board remains fixed at 1440횞988 with continuous fit-scale.
- **Player progress goes through `src/ui/storage.ts` ??never `localStorage` directly (file-based saves, 2026-07-29; three slots 2026-07-31; Emoji progress 2026-08-05):** the UI has exactly one persistence path. `SAVE_KEYS` in that file lists the nine progress keys (`wj.run`, `wj.collection`, `wj.collectionSeen`, `wj.lifetime`, `wj.unlocks`, `wj.vouchers`, `wj.emojiUnlocks`, `wj.tutorial`, `wj.tutorialIntro`); `storage.ts` multiplexes those values through three profile slots, with legacy flat values treated as P1. Profile creation/name and Reveal-All warning/applied/Challenge-disabled markers live inside each slot's `wj.lifetime`: P1 exists as `P1` by default, while P2/P3 are empty until created and blank creation names fall back to their `P#`. Profile tabs and creation only preview the explicit slot; **only Load Profile may change `wj.profile`**. The active slot shows disabled Current Profile and Delete Profile buttons; only a non-active created slot can be deleted, after two presses (warning, then exact-slot deletion). Explicit-slot reads/writes must be used by profile management so preview, deletion, or Reveal All never changes or contaminates another profile. On desktop the main process writes them to `%APPDATA%/Play the World/saves/{run,profile}.json`, which Steam Cloud syncs. The profile file additionally owns one main-process-only root `steamOwner`; it is outside `SAVE_KEYS`, profile slots, renderer snapshots, and generic write/remove IPC, and ordinary rewrites must preserve it. The active slot (`wj.profile`) and everything else (`wj.settings`, `wj.lang`, `wj.sortMode`) are machine-local preferences and stay in `localStorage` everywhere. A missing or invalid `wj.lang` may use only a sanitized `en|ko|ja|zh-CN|zh-TW|pt-BR|de|es-ES|fr-FR|ru-RU|pl-PL|tr-TR` hint from a successfully initialized packaged Windows Steam session for the first render; Steam `japanese` maps to `ja`, `schinese` maps to `zh-CN`, `tchinese` maps to `zh-TW`, `brazilian` maps to `pt-BR`, `german` maps to `de`, `spanish` maps to `es-ES`, `french` maps to `fr-FR`, and `russian` maps to `ru-RU`, and `polish` maps to `pl-PL`, and `turkish` maps to `tr-TR`; detection is never persisted, and raw Steam locale plus Steam id remain main-process-only. **Adding a persisted key means deciding which side it belongs on** ??and `desktop/save-store.js` keeps a duplicate of `SAVE_KEYS` because `desktop/` cannot import from `src/`, so update both (a test fails if they drift). `run.json` and `profile.json` are separate on purpose: `persist.ts` DISCARDS a run whose `VERSION` moved, and one shared file would take the collection and lifetime stats with it. Reads must stay synchronous ??the desktop backend loads once at boot through the preload bridge and serves reads from memory. `desktop/preload.cjs` must stay `.cjs`: a sandboxed preload has to be CommonJS even though the rest of the shell is ESM.

- **Score Keyboard panel lighting (changed 2026-09-03):** every physical key has a deterministic fixed six-colour LED slot. Only pressed keys emit hard-edged pixel gap light. Tier 4?? pressed sets cover all six colours and every pressed key owns a beat-synchronised pixel flame; Tier 5 flames are 25% larger and may add one ember. Tier 4?? also blink the three red/yellow/green indicator LEDs painted in the keyboard image during active and clear-held beats; each beat/cycle assigns their staggered phases in a deterministic random-looking order, while Tier 0?? leave them off. The former central flame, full-keyboard rainbow ring, and jackpot sparks are retired. Tier 3 retains smoke and Tier 4?? retain smoke/POP. Panel LED/flame/key colours are always fully coloured regardless of Palette progress or `world-mono`. Reduced Motion freezes panel LEDs at low intensity and hides moving key-flame VFX while retaining the existing static pressed/Enter accent; Forced Colors hides decorative panel LEDs/key-flame. Preserve the updated visual-key counts and existing audio counts, sampled mapping, replay lifecycle, engine/RNG/storage, and settle timing.

## Spec-conflict protocol (always apply)

The docs (`CLAUDE.md` + `docs/`) are the source of truth. When an instruction from the user conflicts with them ??or two docs conflict with each other, or a request isn't covered at all ??do NOT silently pick one. Instead:

1. **Stop and surface it.** Name the specific conflict: quote the doc rule (with its file + section) and the instruction that contradicts it. Don't bury it in a long reply ??lead with it.
2. **Ask for the decision.** Offer the trade-offs briefly and ask which way to go. If the user's instruction is clearly the newer intent (they're explicitly changing direction), confirm that reading rather than assuming.
3. **Update the docs in the SAME change.** Once the user decides, edit the affected doc(s) so they match the decision ??GDD/UI_DESIGN/screens-spec for design, this file for rules/architecture. Fix every stale cross-reference in the same pass (see principle 6). The decision is not "done" until the docs reflect it; code and docs land together.
4. **Record why, briefly.** For non-obvious reversals, leave a one-line note (a changelog bullet or an inline "(changed: ??" ) so the reasoning survives.

Never implement against a doc rule without doing this. A silent divergence between code and docs is the failure mode to avoid ??the docs must always describe what the code actually does.

## Build order (vertical slices ??implement in this order)

1. **Slice ??Core loop:** `rng.ts`, `bag.ts` (build from `BALANCE.bagComposition`, shuffle, draw), `lexicon.ts` (word `Set` from `data/dictionary.txt` + `data/lexicon.json` lookup), hand/discard state machine, word submission with letter-chip scoring + gibberish path. Tests: bag counts, discard budget, gibberish scoring.
2. **Slice ??Suits + projection:** suit multiplier application, committed vs projected score split, early-end trigger (`projected >= target`).
3. **Slice ??Sentences:** `patterns.ts` ??POS assignment search, whole-sequence matchers for the 12 patterns, modifier absorption, highest-only, mutually exclusive Unison/register-synergy check; sentence finalization at blind end. This slice makes the game *fun* ??test heavily (RUN횞4 ??chant; PIZZA TASTES GOOD ??descriptive; hole voids pattern and register bonuses).
4. **Slice ??Joker bus + first 6 jokers:** implement `JokerBus` wiring in the pipeline, then jokers #1, #2, #10 (layer 1), #12 (layer 2), #22, #24 (layer 3) as the proof set spanning all hook types.
5. **Slice ??Blind/ante/shop:** blind state machine (small/big/boss), target curve, gold flows (clear reward / remaining phases / interest / sell), shop stock + reroll, then bosses and consumables.

6. **Slice ??UI (play screen):** follow `docs/UI_DESIGN.md` and `docs/screens-spec.md` ??together they are the visual contract for tokens, layout, components, flow, and juice priority. React + plain CSS custom properties (no Tailwind on game screens). The UI only reads engine state snapshots and replays the engine's per-submission score-event log for the settle animation; never put game rules in components. **Art direction: pixel-art + CRT** (Balatro lineage; the earlier "ceramic-tile, deliberately un-Balatro" identity and its trade-dress guardrail are RETIRED ??see UI_DESIGN.md). Adopt the pixel-art idiom, CRT post-effect, and screen grammar freely; only avoid copying Balatro's *specific* art assets (its actual card illustrations, logo, exact sprites). Existing components are restyled (pixelation, squared corners, blocky shadows, global CRT pass), not rebuilt ??semantic tokens and layout stay. Pixel-art shop mascot (tuxedo cat proprietor) per UI_DESIGN 짠6.

After each slice, write a tiny scenario in `src/sim/` exercising it headlessly.

## Data files (`data/`)

- `dictionary.txt` ??lowercase ENABLE words up to 18 letters plus documented tile-grammar exceptions, the four curated MVP/VIP surfaces, and exact reviewed omissions from `lexicon-pipeline/curated-validity.json` (GDD 짠3.2), including apostrophe-free `aint` as Slang AIN'T with the auxiliary/linking-verb POS convention. This registry admits only listed spellings; it never opens a proper-name category or generates derivatives.
- `lexicon.json` ??complete baked `{ word: { suit, pos[] } }` table. Register classification follows `docs/# ?곷떒???덉??ㅽ꽣 遺꾨쪟 湲곗?.md`; every entry has non-empty POS. Curated acronym metadata stays in `lexicon-pipeline/curated-abbreviations.json`, reviewed validity omissions stay in `curated-validity.json`, and both baked outputs consume both registries while runtime keeps its existing two-file loader.
- The reproducible POS/register pipeline lives in `lexicon-pipeline/` and runs offline only. Never fetch or classify words in the game runtime.

## Stack

- TypeScript (strict), Vite, Vitest.
- UI: React for the prototype; engine/renderer separation means it can be swapped for PixiJS later if animation demands grow.
- No server; everything is client-side. Profile-scoped progress uses the implemented `src/ui/storage.ts` adapter, backed by localStorage on the web and JSON files in the desktop build.

## Conventions

- English identifiers mirroring GDD English names (`nameKo` field preserves Korean display names).
- One joker per file under `src/engine/jokers/`, grouped by rarity in an index.
- Tests colocated in `tests/`, named by slice (`slice1-bag.test.ts`, ...).

**Score Keyboard jackpot/smoke follow-up (changed 2026-09-03):** the red/yellow/green panel LEDs keep their deterministic random-looking phase order but use rapid hard-edged jackpot pulses. Each overlay centre aligns with its painted indicator while the light casts wider horizontal rays. Tier 3?? key smoke rises from each pressed cap; per-key smoke and flame use deterministic 0.95??.05 variation on a larger visible base, with Tier 5 retaining 횞1.25 flame and 횞1.35 key-smoke multipliers. Tier 4 also emits seven small distributed chassis-smoke particles; Tier 5 emits all twelve at a larger size in dark gray. No single smoke overlay is allowed. Reduced Motion and Forced Colors hide moving/decorative smoke, flame, and panel VFX.
