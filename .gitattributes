# Normalize line endings: store text as LF in the repo, check out native.
# Prevents cross-platform CRLF churn (this repo is developed on Windows).
* text=auto eol=lf

# Explicitly binary (never touch these).
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
