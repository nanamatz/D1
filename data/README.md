# Runtime lexicon data

`dictionary.txt` and `lexicon.json` are the baked offline outputs used by both
Node simulations and the browser/desktop bundle. They are not runtime-generated
or fetched. Run `npm run check:data` after changing either file.
